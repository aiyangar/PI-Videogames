

let initialState = {
  allVideogames: [],
  allGenres: [],
  allPlatforms: [],
}

function rootReducer(state = initialState, action) {

  switch (action.type) {
    
    default:
      return state;
  }

}

export default rootReducer;